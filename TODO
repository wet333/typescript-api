# TODO

· Implement the builder pattern for making new endpoints, so i can add new features to each endpoint in the future **Super failed first try**

· Make a real secret for the sessions

· Create a new type for DatabaseErrors, add the query that failed to the error response
